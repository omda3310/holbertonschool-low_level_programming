#include "main.h"
#include <stdio.h>
/**
 * mul - check the code
 *
 * @a : check the code.
 * @b : check the code.
 *
 * Return: Always 0.
 */
int mul(int a, int b)
{
	int r;

	r = a * b;
	return (r);
}
